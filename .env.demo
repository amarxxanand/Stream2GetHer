# Demo/Development Environment Configuration
# Copy this to .env and update with your actual values

# Server Configuration
NODE_ENV=development
PORT=3000
CLIENT_URL=http://localhost:5173

# MongoDB Configuration (update with your MongoDB connection)
MONGODB_URI=mongodb://localhost:27017/stream2gether

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Google Drive API Configuration
# IMPORTANT: You need to set up Google Drive API credentials
# Follow the setup guide in MIGRATION-GUIDE.md

# Option 1: Service Account (Recommended)
# Replace with your actual service account JSON key (as a single line)
GOOGLE_SERVICE_ACCOUNT_KEY=

# Option 2: OAuth2 (Alternative)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback
# GOOGLE_REFRESH_TOKEN=your-refresh-token

# Video Configuration
MAX_VIDEO_SIZE_GB=5

# Security (generate a secure random string for production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.demo .env
# 2. Follow MIGRATION-GUIDE.md to set up Google Drive API
# 3. Update MONGODB_URI if using a different MongoDB instance
# 4. Set GOOGLE_SERVICE_ACCOUNT_KEY with your service account JSON
# 5. Run: npm run dev
