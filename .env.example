# Server Configuration
NODE_ENV=development
PORT=3000
CLIENT_URL=http://localhost:5173

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/stream2gether

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Google Drive API Configuration
# Option 1: Service Account (Recommended for production)
# Create a service account in Google Cloud Console and download the JSON key
# Then set this environment variable to the entire JSON content (as a string)
GOOGLE_SERVICE_ACCOUNT_KEY=

# Option 2: OAuth2 (For development/personal use)
# Create OAuth2 credentials in Google Cloud Console
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback
GOOGLE_REFRESH_TOKEN=

# Video Configuration
MAX_VIDEO_SIZE_GB=5

# Security
JWT_SECRET=your-super-secret-jwt-key-here

# Instructions:
# 1. Copy this file to .env
# 2. Set up Google Drive API credentials:
#    - Go to Google Cloud Console (https://console.cloud.google.com/)
#    - Create a new project or select existing one
#    - Enable Google Drive API
#    - Create credentials (Service Account recommended)
#    - Download the JSON key and paste the entire content as GOOGLE_SERVICE_ACCOUNT_KEY
# 3. Start your application with: npm run dev
